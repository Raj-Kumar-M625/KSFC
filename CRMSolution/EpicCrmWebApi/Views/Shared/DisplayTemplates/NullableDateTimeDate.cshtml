@model DateTime?

@{
    if (Model.HasValue && Model.Value != DateTime.MinValue && Model.Value.Year != 1900)
    {
        @String.Format("{0:ddd dd-MM-yyyy}", Model)
    }
    else
    {
        @String.Empty
    }
}
